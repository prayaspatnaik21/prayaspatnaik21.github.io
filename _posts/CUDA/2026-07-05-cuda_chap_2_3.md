---
layout: post
title: CUDA Notes Part I
date: 2026-07-05
category: "GPU , CUDA"
reading_time: 10
icon: "CUDA"
status : Work In progress
excerpt: "Notes from Progamming Massively Parallel Processors Book."
---

# CUDA Part - I
---------------


1. What is a kernel?
    1. Piece of device code which is executed by large number of threads.

2. Memory Address in device memory should not be dereferenced in the host code. Dereferencing a device global memory pointer in the host code can cause exceptions or other types of runtime errors.

3. Threads are collectively called grid.
4. when a program's host code calls a kernel , the CUDA runtime system launches a grid of thread that are organized into two level hierarchy.
    1. Each grid is organized as an array of thread blocks.
    2. Host code can specify the no. of threads in the block.
    3. Same kernel can be called with different number of threads at different part of the host code.
5. The no. of threads in a block is available in built-in variable 'blockDim'.Blocks are 3D structure. We can organize the threads in 3 dimensional inside block.
6. The choice of dimensionality for organizing threads usually reflect the dimensionality of the data.
7. Basically , threads are organized in blocks , blocks are organized in Grids. 
8. CUDA gives you built in variable to access a particular thread in a Grid. 
    1. threadIdx and blockIdx , threadIdx is index of the thread in a block and blockIdx is the index of the block in a grid.
    2. All the threads in the block has a common blockIdx.
    3. For calculation purpose - global index i = threadIdx.x + blockDim.x * blockIdx.x
9. If a function is declared in CUDA C kernel function as __global__ . The function is executed on device but can be called from host.
10. If a function is declared in CUDA C kernel function as __device__. The CUDA device function executes on CUDA device and can be called only from a kernel function or another device function.
11. Automatic variables are private to each thread.
12. Multi- Dimensional Grids and Data
    1. Block is a 3D array of threads.
    2. Grid is a 3D array of blocks.
    3. gridDim -> tells you the number of blocks. If the code is using gridDim.x , number of blocks in x direction. similarly we have gridDim.y and gridDim.z.
    4. Point 3 is true for blockDim as well. It tells you the number of threads in a block . blockDim.x tells you the number of threads in x direciton. similarly , we have blockDim.y and blockDim.z.
    5. All the blocks in the grid have the same size and dimensions.
    6. The total size of the a block in current CUDA system is limited to 1024 threads.
    7. Kernel_function <<< dimGrid , dimBlock >>> (...).
13. Mapping Threads to Multi-Dimensional Data
    1. The choice of 1D , 2D , 3D thread organization is usually based on the nature of the data.
    2. Using a 2D grid that consist of 2D blocks is often convenient for processing the pixels.
    3. Finding the coordinate if code is using a 2D grid.
        1. Vertical (row) coordinate = blockIdx.y * blockDim.y + threadIdx.y
        2. Horizontal (column) coordinate = blockIdx.x * blockDim.x + threadIdx.x

# Exercies

1. If we want to use each thread in a grid to calculate one output element of a vector addition , what would be the expression
for mapping the thread/block indices to the data index (i) in our kernel function?

    1. i = threadIdx.x + blockIdx.x * blockDim.x;
    
2. Assume that we want to use each thread to calculate two adjacent elements of vector addition. What would be the expression
for mapping the thread/block indices to the data index (i) of the first element to be processed by a thread?
    
    1. For each of the thread , the starting indices should be even , starting from 0 , 2 , 4 , 6 ....
    2. i = (blockIdx.x * blockDim.x + threadIdx.x) * 2.
    3. Given the total number of threads will be half of the number of the elements in the vector and each thread will process two adjacent elements in the vector.

3. We want to use each thread to calculate two elements of a vector addition. Each thread block process 2 * blockDim.x consecutive elements that form two sections. All threads in each block will first process a setion first , each processing one
element. They will then move to the next section , each processing one element. Assume that variable i should be the index for the first element to be processed by a thread. What would be the expression for mapping the thread/block indices to data index of the first element?
    
    1. each thread gives you the output of two element addition.
    2. Each thread block process 2 * blockDim.x consecutive elements that form two sections.
    3. Above statement is little confusing to me. Based on my understanding , it's saying that the thread in block are responsible for producing results for 2 * blockDim.x elements. Suppose there are 4 threads in a block , therefore these 4 threads are responsible for producing the vector addition of 8 elements in the vector. (2 * blockDim.x -> 2 * 4).
    4. Catch is they will first process one section first and then move on to second. Thread won't be able to progress to second set of elements with out finishing the first set of elements.
    5. Mapping the thread/block indices to data index of the first element
        1. 1st thread -> id - 0 , 4 
        2. 2nd thread -> id - 1 , 5 
        3. 3rd thread -> id - 2 , 6
        4. 4th thread -> id - 3 , 7
        5. i = threadIdx.x + blockIdx.x * blockDim.x * 2

4. For a vector addition , assume that the vector length is 8000 , each thread calculates one output element , and the thread block side is 1024 threads. The programmer configures the kernel call to have minimum number of thread blocks to cover all output elements. How many threads will be in the grid?
    
    1. Constraint -> minimum number of thread blocks.
    2. Use the full capacity of the block and that is 1024.
    3. Number of blocks required = 8000/1024 = 7.8125 -> 8 blocks 
    4. 7 blocks will contain 1024 thread each which is 7168 and the last block will contain 1024 threads with 832 active and 192 inactive.
    5. The answer is 8192.

5. If we want to allocate an array of v integer elements in CUDA device global memory , what would be an appropriate expression for the second argument of the call to cudaMalloc?

    1. v * sizeof(int)

6. If we want to allocate any array of n floating point elements and have a floating pointer variable A_d to point to the allocated memory. what would be an appropriate expression for the first argument of the cudaMalloc call?
     
    1. (void ** ) &A_d

7. If we want to copy 3000 bytes of data from host array A_h (A_h is a pointer to element 0 of the source array) to device array A_d (A_d is a pointer to the element 0 of the destination array) , what would be an appropriate API call for this data copy in CUDA?

    1. cudaMemcpy(A_d, A_h, 3000, cudaMemcpyHostToDevice)

8. How would one declare a variable err that can appropriately receive the returned value of a CUDA API call?
    
    1. cudaError_t err;

9. Consider the CUDA Kernel and the corresponding host function that calls it - image is in book

    1. what is the number of threads per block?
        
        1. function <<< blockDim , gridDim>>>(...) based on the notes above.
        2. Threads per block -> blockDim
        3. foo_kernel <<< (N + 128 - 1) / 128 , 128>>> (a_d , b_d , N)
        4. Thread per block -> (200000 + 128 - 1) / 128 = 1563.49 -> 1564
    2. what is the number of threads in the grid?
        1. Number of threads in a grid = number of blocks in a grid * number of threads in a block
        2. Number of threads in a grid = 128 * 1564 = 200192

    3. what is the number of blocks in the grid?
        1. 128 is the number of blocks in a grid

    4. what is the number of threads that execute the code on line 02?
        1. 200192

    5. what is the number of threds that execute the code on line 04?
        1. 200000

10. A new CUDA programmer is frustrated with CUDA. He has been complaining that CUDA is very tedious: he had to declare many   functions that he plans to execute on both the host and the device twice, once as a host function and once as a device function. How would you help this new programmer?

    1. I would ask him some clarification about why he wants to implement a same algorithm on both host side and device side.
    2. Is it for benchmarking ?
    3. On a very high level , based on this chapter , the design flow for writing a simple CUDA code is as follows:
        1. Allocate memory for input and output.
        2. Transfer memory to device.
        3. Define the kernel function and call the kernel function for computation.
        4. Transfer the output result from device to Host side.
        5. Free the Device memory.

# ToDo

    1. write the code for Question 1
    2. write the code for Question 2
    3. write the code for question 3
    4. Add chapter names